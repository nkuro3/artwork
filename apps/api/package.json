{
  "name": "@artwork/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@artwork/database": "workspace:*",
    "@artwork/shared": "workspace:*",
    "hono": "^4",
    "better-auth": "latest"
  },
  "devDependencies": {
    "@artwork/config": "workspace:*",
    "@cloudflare/workers-types": "^4",
    "wrangler": "^3",
    "typescript": "^5"
  }
}
